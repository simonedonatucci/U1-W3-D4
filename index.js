const Array = function () {
  const arr = [];
  for (let i = 0; i < 76; i++) {
    arr.push(i + 1);
  }
  return arr;
};
const randomNumber = function (casual) {
  const randonNUm = Math.floor(Math.random() * casual.length);
  const randomSplice = range.splice(randIndex, 1)[0];
  return random;
};
